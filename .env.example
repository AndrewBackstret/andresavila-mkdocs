# Copy this file to .env and fill in your values.
# .env is in .gitignore — never commit it to the public repo.

# Google Analytics (optional)
# Used in mkdocs.yml as: property: !ENV GOOGLE_ANALYTICS_ID
GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Add any other secrets here (API keys, tokens, etc.)
